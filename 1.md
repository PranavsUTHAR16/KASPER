graph TD
    %% Input Layer
    Input["Raw Financial Features (Φ_t)"]

    %% Split to both layers
    Input --> L1_Spline
    Input --> L2_Spline

    %% ==========================================
    %% LAYER 1: REGIME DETECTION
    %% ==========================================
    subgraph Layer1 ["Layer 1: Regime Detection Layer"]
        L1_Spline["Spline Transformation (Quantile Initialized)"]
        L1_Deep["Deep Block: (Linear → BN → GELU → Dropout) × 2"]
        L1_Latent["Latent Embeddings (Z_t)"]
        L1_Proj["Linear Regime Projection"]
        L1_Gumbel["Gumbel-Softmax Router (Eq. 17)"]
        L1_Probs{"Regime Probabilities (P_t)"}

        L1_Spline --> L1_Deep
        L1_Deep --> L1_Latent
        L1_Latent --> L1_Proj
        L1_Proj --> L1_Gumbel
        L1_Gumbel --> L1_Probs
    end

    %% KASPER Master Model
    subgraph LD["KASPER Layer 1: Regime Detection (regime_detection.py)"]
        D1[3D Tensor Input: Batch, 10, 8]
        D2[Flatten features: Batch, 80]
        D3[SplineActivation: Linear + Cubic quantile knots]
        D4[Embeddings z_i: Batch, 64]
        D5[Classifier MLP: BatchNorm -> GELU -> Dropout]
        D6[Regime Logits: Batch, 3]
        D1 --> D2
        D2 --> D3
        D3 --> D4
        D4 --> D5
        D5 --> D6
    end
    C3 --> D1

    %% Routing
    subgraph RAR["Regime Assignment & Routing (evaluate.py)"]
        E1{Evaluation Mode?}
        E2[Softmax Routing: Soft Probabilities p_i]
        E3[Argmax: Hard Regime Assignments active_regimes]
        E4[Regime Distribution: Counts / Percentages]
        
        D6 --> E1
        E1 -- Yes --> E3
        E1 -- No / Training --> E2
        E3 --> E4
    end
