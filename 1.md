graph TD
    %% Input Layer
    Input["Raw Financial Features (Φ_t)"]

    %% Split to both layers
    Input --> L1_Spline
    Input --> L2_Spline

    %% ==========================================
    %% LAYER 1: REGIME DETECTION
    %% ==========================================
    subgraph Layer1 ["Layer 1: Regime Detection Layer"]
        L1_Spline["Spline Transformation (Quantile Initialized)"]
        L1_Deep["Deep Block: (Linear → BN → GELU → Dropout) × 2"]
        L1_Latent["Latent Embeddings (Z_t)"]
        L1_Proj["Linear Regime Projection"]
        L1_Gumbel["Gumbel-Softmax Router (Eq. 17)"]
        L1_Probs{"Regime Probabilities (P_t)"}

        L1_Spline --> L1_Deep
        L1_Deep --> L1_Latent
        L1_Latent --> L1_Proj
        L1_Proj --> L1_Gumbel
        L1_Gumbel --> L1_Probs
    end

    %% ==========================================
    %% LAYER 2: REGIME-ADAPTIVE FORECASTING
    %% ==========================================
    subgraph Layer2 ["Layer 2: Regime-Adaptive Forecasting Layer"]
        L2_Spline["Regime-Specific B-Spline Basis (Eq. 21)"]
        L2_Weights["Raw Learnable Weights (w)"]
        L2_Threshold["Soft-Thresholding Gate (Eq. 22)"]
        L2_SparseWeights["Sparse Active Weights"]
        L2_Forecasts["Regime-Specific Forecasts (y_hat^r) (Eq. 20)"]

        L2_Weights --> L2_Threshold
        L2_Threshold --> L2_SparseWeights
        L2_Spline --> L2_Forecasts
        L2_SparseWeights --> L2_Forecasts
    end

    %% ==========================================
    %% AGGREGATION & LOSS
    %% ==========================================
    L1_Probs --> Aggregation
    L2_Forecasts --> Aggregation
    Aggregation(("Weighted Sum")) --> Output["Final Price Forecast (y_hat_t)"]

    %% ==========================================
    %% COMPOSITE LOSS FUNCTION
    %% ==========================================
    subgraph CompositeLoss ["The KASPER Composite Loss (Eq. 23)"]
        Loss_Huber["L_Huber (Accuracy)"]
        Loss_Cont["L_Contrastive (Clustering)"]
        Loss_Orth["L_Orthogonal (Separation)"]
        Loss_Sparse["L_Sparsity (L1 Penalty)"]
        Loss_Bal["L_Balance (Regime Distribution)"]
    end

    Output -.-> Loss_Huber
    L1_Latent -.-> Loss_Cont
    L1_Proj -.-> Loss_Orth
    L2_SparseWeights -.-> Loss_Sparse
    L1_Probs -.-> Loss_Bal
