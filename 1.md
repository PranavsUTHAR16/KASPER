graph TD
    %% Input Data
    subgraph RawInputs["Raw Inputs"]
        A1[Raw SPY Data: Open, High, Low, Close, Volume]
    end

    %% Feature Engineering
    subgraph FE["Feature Engineering (preprocess_spy.py)"]
        B1[Calculate Base Features: Spreads, Log Returns]
        B2[Calculate Rolling Metrics: Volatility, Velocity, ATR]
        B3[Calculate Regime Context: Momentum, Acceleration]
        B4[One-Hot Temporal Encodings: Day of Week, Month]
        B1 --> B5[Assemble Candidate DataFrame: 33 features]
        B2 --> B5
        B3 --> B5
        B4 --> B5
    end
    A1 --> B1

    %% Feature Selection & Preparation
    subgraph FSS["Feature Selection & Scale"]
        C1[SelectKBest & f_regression: Top 8 Features]
        C2[Temporal Sequence Packing: Window size T=10]
        C3[Standardize Features: Mean 0, Variance 1]
        B5 --> C1
        C1 --> C2
        C2 --> C3
    end

    %% KASPER Master Model
    subgraph LD["KASPER Layer 1: Regime Detection (regime_detection.py)"]
        D1[3D Tensor Input: Batch, 10, 8]
        D2[Flatten features: Batch, 80]
        D3[SplineActivation: Linear + Cubic quantile knots]
        D4[Embeddings z_i: Batch, 64]
        D5[Classifier MLP: BatchNorm -> GELU -> Dropout]
        D6[Regime Logits: Batch, 3]
        D1 --> D2
        D2 --> D3
        D3 --> D4
        D4 --> D5
        D5 --> D6
    end
    C3 --> D1

    %% Routing
    subgraph RAR["Regime Assignment & Routing (evaluate.py)"]
        E1{Evaluation Mode?}
        E2[Softmax Routing: Soft Probabilities p_i]
        E3[Argmax: Hard Regime Assignments active_regimes]
        E4[Regime Distribution: Counts / Percentages]
        
        D6 --> E1
        E1 -- Yes --> E3
        E1 -- No / Training --> E2
        E3 --> E4
    end
